spring:
  application:
    name: springboothazelcast
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password: sa
    platform: H2
  jpa:
    hibernate:
      ddl-auto: create-drop
server:
  port: 8082
